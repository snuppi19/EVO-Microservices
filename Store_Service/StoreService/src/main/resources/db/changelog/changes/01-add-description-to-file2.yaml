databaseChangeLog:
  - changeSet:
      id: 1
      author: mtran
      changes:
        - addColumn:
            tableName: file2
            columns:
              - column:
                  name: description
                  type: varchar(500)
                  constraints:
                    nullable: true
      comment: Add 'description' column to the 'file' table